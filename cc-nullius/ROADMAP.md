# cc-nullius — day-to-day readiness tracker

Blind-spot audit 2026-07-18, after the prevention-vs-compaction A/B.
Ranked by expected daily pain. Status: OPEN / IN-PROGRESS / DONE (date).

| # | Blind spot | Why it hurts daily | Countermeasure | Status |
|---|---|---|---|---|
| 1 | All evidence is headless single-mandate runs; interactive dynamics unmeasured (mid-task scope changes, user-driven reads, interrupted hunts) | Doctrine has no re-gating rule when the mandate shifts mid-session | Dogfood + add a "mandate shifted → re-run the gate" rule once failure modes are observed, not guessed. Interactive doctrine shipped 2026-07-19: post-terrain GAP CHECK (one batch, cap 4, three-part qualification, asked in the Turn-B dispatch turn so hunts never block), one-way-door rule (block only on irreversible choices; else PROVISIONAL), user-answers-are-testimony (intent binds, facts verified), mandate-shift re-entry (scope-changing answer → delta Turn A + re-rule gate), gap ledger + close ratification. Research-hardened 2026-07-19 (4-agent web sweep): (a) overlap VERIFIED — background dispatches run detached from the leader loop; AskUserQuestion blocks only the leader and is unavailable to subagents (Claude Code docs; Cursor 2.4 ships the same pattern; LangGraph interrupts freeze siblings — avoided); (b) reversibility = 4-layer escape analysis (read / undo-artifact→PROVISIONAL / escapes-worktree→BLOCK / unclassifiable→BLOCK), from Krakovna reachability + Anthropic auto-mode tiers + buf-breaking-class detectors; "reversibility decays" → flush at close; (c) silence splits by layer, lazy-consensus style (Apache/Rust FCP precedent): layer-2 stands-unless-objected with evaporation, layer-3 never silent-ratifies; (d) ledger salience = recite at tail of every update (goal-restating measurably cuts drift, p<0.05; edge placement is the robust lost-in-the-middle fix; close-time re-read alone is confirmation theater — 85-95% of rechecks confirm rather than correct). Remaining: pure dogfood (does it FEEL right in real sessions) | PARTIAL 2026-07-19 |
| 2 | No triviality tier: governor + ceremony engage on every task, incl. quick questions and 3-line fixes | Grep denials and steering noise on tasks with nothing to hunt; only escape is the blunt `.nullius-off` | QUICK mode: `.nullius-quick` marker (4h auto-expiry) set/cleared by `/nullius:quick`; governor drops to diet-lite (sweeps+reads allowed, Bash still tail-bounded); SKILL gets a QUICK ruling at the gate | DONE 2026-07-18 |
| 3 | Lens library is concurrency-shaped and Go-flavored; empty terrain on TS/SQL/infra work | Every hunt outside Go-like domains is wasted or BUILD-gated | Lens-derivation method → always-on lens library (bench-9 rev3 design, unbuilt); derive lenses per domain from misses | OPEN (blocked on bench-9 rev3) |
| 4 | MCP tools bypass the diet: bulk server responses land unbounded in the leader | One browser-HTML or Drive fetch can out-bloat everything the governor prevents | Main-thread MCP calls with bulk-verb names steered to scouts (subagents reach MCP via ToolSearch); `NULLIUS_MCP_OK=1` or QUICK mode to disable | DONE 2026-07-19 (gate logic 07-18 was DEAD — hooks.json matcher omitted `mcp__*`, so the hook never fired for MCP; test-drive caught it; matcher + wiring-guard test added, verified end-to-end via stats file) |
| 10 | Craftsman tests-first + boundary gates never bound under plugin install | The plugin's core safety feature was fully bypassed — craftsman edited source ungoverned | `agent_type` arrives namespaced `nullius:nullius-craftsman` under marketplace install (bare only via the harness's `.claude/agents` copy); a `===` fell through to the subagent passthrough. Fixed to regex `/(^\|:)nullius-craftsman$/`; verified end-to-end | DONE 2026-07-19 |
| 5 | No cross-session persistence: same terrain re-absorbed every session in the same repo | Session 2+ pays full absorption for unchanged code | Terrain cache `.nullius/terrain.md` (commit-stamped); Turn A validates freshness via git diff and delta-scouts only the drift; close refreshes it | DONE 2026-07-18 (doctrine) |
| 6 | Repos without test infra silently weaken tests-first + the close record | Close degrades to build+vet without saying so | Close must NAME the missing suite as a RISK (close.md step 3 + SKILL step 6) | DONE 2026-07-18 |
| 7 | No in-plugin cost/dispatch telemetry | Flying blind on the economics the whole project is about | Governor counts denies/rewrites/dispatches per session (`/tmp/nullius-stats-<session>`); `/nullius:diet status` reports them | DONE 2026-07-18 |
| 8 | Wrong REFUTED on trusted hunter testimony (measured: wake-predicate 5/6) | A quoted-but-non-covering mechanism clears a real defect | REFUTED on a core lens needs leader-read lines or a pinning test | DONE 2026-07-18 (validated same day: 6/6) |
| 9 | Mechanical cross-file renames route through craftsman + tests-first | Churn for a mechanical change | QUICK mode now covers this; a rename-aware Edit exemption would be cleaner | PARTIAL (via #2) |
| 11 | Governor always-on, but the SKILL body loads only on invocation → a session can be starved yet not run the method | "Installed" did not mean "doctrine applied"; relied on the model reaching for `/nullius` | SessionStart hook injects a ~70-token pointer (not the ~5k body) nudging the orchestrator to invoke the skill on nontrivial work; respects the off switches; verified a fresh session receives and reads it | DONE 2026-07-19 |
| 12 | Ratchet counts per-edit, not per-turn: a well-batched message (tests + source in one turn) trips the 4-edit limit mid-batch and denies the tail — even when a test Write is in the same message. Governor fights the doctrine's own batching rule (observed live 2026-07-22, go-nullius session) | Same class as counsel #6 boundary-gate gaps: the letter of the diet vs its own doctrine; punishes exactly the batching the doctrine demands | The hook payload exposes no turn/message id, so true per-turn grouping is impossible per-call. Adopted the robust proxy: a stat-based mtime WATERMARK — a test touched earlier in the same batch (by any tool) advances newestTestMtime past the watermark and resets the counter on the next source edit; doctrine still orders test writes first. #14's idempotent-retry closes the residual source-before-test case | DONE 2026-07-24 |
| 13 | Test detection is tool-blind: a test appended to `*_test.go` via Bash heredoc doesn't register — hook only watches Edit/Write, forcing a redundant Write of a new test file to reset the counter (observed live 2026-07-22) | Model pays a wasted write + turn to satisfy a counter the work already satisfied | `newestTestMtime(cwd)` — a bounded stat-based walk (skips VCS/dep/build dirs, caps entries), go-nullius's staleness Tracker in miniature. A `*_test`/`.spec` file freshened by ANY tool (Bash heredoc/redirect included) resets the ratchet on the next source edit | DONE 2026-07-24 |
| 14 | Denials are all-or-nothing with no idempotent retry hint: the same blocked edits must be re-sent verbatim across turns — burning exactly the turns the doctrine bills as the other cost | Denial churn is pure turn tax; milder than #12/#13 but compounds with them | Denial message now states the reset is tool-agnostic (Edit/Write OR a Bash write to a test file) and that the blocked edits pass UNCHANGED on retry once the test lands — and the watermark makes that literally true (the re-sent source edit sees the fresh test mtime and resets) | DONE 2026-07-24 |
| 15 | The diet governs CONTEXT but nothing governs REASONING EFFORT, and turn-batching × high effort concentrates spend. Measured (interactive vialite drive, 2026-07-24, `benchmarks/2026-07-24-cc-nullius-interactive-vialite-drive.md`): a perfectly lean run (ctx ≥70% free throughout) still cost heavily because Fable-5 @ xhigh turned the batched build into one ~23min/~90k-token reasoning turn. nullius's "few big turns" (right for cache) multiplies xhigh's huge per-turn reasoning | A lean context does NOTHING to cap reasoning cost; the two levers are orthogonal and a cost-conscious user can be lulled by a healthy ctx% while effort silently dominates | Doctrine should name effort as a first-class cost lever (Hygiene): default the daily-driver to a modest effort, and note the batching×effort interaction so the leader doesn't over-reason on mega-turns. Possibly a launcher hint in `bin/nullius` (warn if effort is xhigh) | OPEN 2026-07-24 |
| 16 | Switching `/effort` mid-run invalidates the prompt cache → full-history re-read. Observed live 2026-07-24: the confirmation modal itself warns of it; switching late (after the expensive turns) costs a re-cache that exceeds the saving | A cost-reduction action becomes a cost-INCREASING one when taken late; the naive "it's getting expensive, drop effort" instinct backfires | Doctrine/launcher guidance: set effort BEFORE the run; only switch when substantial expensive turns remain. `bin/nullius` could accept/prompt an effort at launch so mid-run switches are never needed | OPEN 2026-07-24 |
| 17 | Compaction was handled by PROSE ONLY: ctx-sentinel and SKILL step 7 told the user to type `/compact …`, so an AUTO-compaction (or a user who compacts without reading the nudge) destroyed the ruled checklist and verification record with nothing on disk to restore | The one event that reliably erases a hunt's results had no mechanical countermeasure — exactly the class the plugin exists to remove | Researched the real levers: no plugin can trigger or shape compaction (#37307/#58538 closed "not planned"; `PreCompact` blocks but cannot rewrite the summary), but `SessionStart` with `matcher: "compact"` fires post-compaction and its stdout enters the fresh context. Shipped `/nullius:compact` (leader distills `.nullius/ledger.md`) + `hooks/compact-reinject.mjs` (re-injects it, ledger declared authoritative over the summary; missing ledger warns loudly). Deliberately never blocks compaction — a block at the ceiling could trap the session. +11 tests, suite 55 green | DONE 2026-08-17 |
| 18 | The compaction loop still had a HUMAN in it: the sentinel asked the user to type `/compact …`, and the platform's own auto-compact only fires near the model's full window (~967k on Opus 5) — ~840k past the 128k attention knee | An automatic countermeasure that depends on a copy-paste is not automatic; and left to the platform, compaction happens long after judgment has degraded | Two halves, no typing: (a) plugin commands are model-invocable via the Skill tool, so the ctx-sentinel now instructs the model to call `Skill("nullius:compact")` itself (verified in docs; a ledger <10min old suppresses further nudges); (b) `bin/nullius` exports `CLAUDE_CODE_AUTO_COMPACT_WINDOW=200000` (env var beats `--autocompact`/`autoCompactWindow`; plain integer only), so compaction fires on its own just past the knee and `compact-reinject` restores the ledger. On-demand triggering remains impossible for hooks/model/SDK — lowering the window is the supported substitute | DONE 2026-08-17 |

| 19 | The self-invoked ledger was ADVISORY and therefore did not happen: across two auto-compaction drives (2026-08-17/18) the ctx-sentinel nudged twice, the session kept serving the user's task, auto-compaction fired between turns and the record was lost both times (`compact:noledger` twice, `ctx:nudge:2`, no ledger on disk) | The whole compaction-survival design rests on a ledger existing BEFORE the ceiling; if writing it depends on the model choosing to interrupt the user's task, it is not a countermeasure | Made it mechanical, in the diet's own deny-then-comply idiom: a LEDGER GATE in diet-governor denies context-filling calls (Read/Grep/Glob/Web*/Agent/Task/mcp__*) past NULLIUS_CTX_KNEE while `.nullius/ledger.md` is missing or >10min stale; Write/Edit/Bash stay open so the ledger is writable under the gate, and the existing escapes (#nullius:ok, .nullius-off, NULLIUS_OFF) prevent trapping. Governor now reads live context from the transcript tail (fail-open: unmeasurable context never gates). Also widened the ctx-sentinel PostToolUse matcher from `Agent|Task` to include Read/Bash/Grep/Glob/Web* — measured: a session that filled its own context by reading files never got a single nudge | DONE 2026-08-18 |

Measured record backing the ranks: benchmarks/NEXT.md, the repo README
table, and the memory ledger. Nothing here is speculative except #1 by
design — it is listed to force measurement before doctrine.

## Crossover measurement (2026-07-19) — write-delegation threshold

Empirical (Docker, no plugin, Opus leader) settled where delegating a
write beats writing it. **Measured Opus 4.8 output ≈ $26/M** (not the
$75/M list price the first analytical pass assumed — the run overturned
it), sonnet $15/M → output gap only ~$11/M (1.7x). Cold-absorption tax
A ≈ $0.41. Crossover `S* = A/gap`: **~1,800 lines cold-dispatch, ~130
lean-dispatch.** Direct proof: cold sonnet cost $30/M-output > Opus's
$27/M writing it one-shot at ~250 lines — cold delegation already loses.
Outcome: removed the `MAX_WRITE`/`MAX_EDIT` hook size-caps (a post-
generation deny double-bills), moved the delegate-or-write decision into
the doctrine (Division of labor), kept tests-first + boundary gates.
Suite 31 green.

# go-nullius — deterministic orchestrator design (fable/xhigh advisor, 2026-07-23)

Design for a pure-Go deterministic driver for weak local models. Reasoned from the
advisor context pack (advisor-pack-v2.md). Verbatim advisor output below.

> **AUTHORITY:** the verbatim advisor body records the *original* reasoning; where it
> and the **AMENDMENTS** section (end of file) disagree, AMENDMENTS win. In particular
> the body's `go/ast`/`go/parser`/`go/types` substrate is **superseded** by tree-sitter
> (see Amendment 1 — settled). Read AMENDMENTS first for current decisions.

---

**Premise disputed:** on a weak model the model should never *find* anything. Go finds;
the model only *discriminates between two concrete alternatives it is shown*. Every
open-ended judgment ("is this a defect?") is where it over-flags. Every contract should be
a forced choice over evidence the orchestrator already extracted. The "leader" as a smart
conversational entity dissolves — what survives is three narrow stateless smart calls
(lens selection, plan authoring, report writing). No leader loop → no leader context to diet.

## Q1. STATE MACHINE — 9 phases, linear + one bounded loop

    Index → Orient → Enumerate → Judge → Corroborate → Plan → Drain → Audit → [defer] Close
    (Go)   (smart×1)  (go/ast)   (fast×N) (Go+fast+esc) (smart×k)(Go+craft)(Go)   (Go,+smart×1)

- **Index** (Go): parse whole module (go/parser+go/types) → Index{Files,Funcs,Types,CallGraph,SharedState}. → Orient.
- **Orient** (smart×1): task + mechanical terrain digest → typed `{lenses:[]LensID (from FIXED library), focus_pkgs, risk_note}`. Model picks WHICH analyzers, cannot invent. Invalid JSON after 2 retries → run ALL lenses. → Enumerate.
- **Enumerate** (Go): run selected AST lenses → `Candidate{ID,Lens,File,Line,Fn,DiskSnippet,ContextSnippet,MechanicalFacts}`. Snippet read from disk by Go → fabrication impossible. Cap 30/lens by mechanical severity. candidates>0→Judge; ==0→Close.
- **Judge** (fast×N parallel, per candidate): forced-choice → `{answer: DEFECT|CORRECT|CANT_TELL, decisive_line: int (∈ offered), because: ≤160c}`. Line out of range → auto CANT_TELL. → Corroborate.
- **Corroborate** (Go+fast): three filters on every DEFECT: (1) decisive-line validity check (repurposed quote-gate — line must contain the lens's expected mechanism token); (2) pair-discrimination for same lens+shape (D2 vs FP): one fast call showing BOTH snippets — "identical shape; which is scope-safe, name the arg carrying scope + where it comes from"; identical undifferentiated answers → both CANT_TELL → escalate smart; (3) refuter `{stands:bool, refuting_line:int|null}`. Smart escalation ≤3/run. confirmed>0→Plan; ==0→Audit.
- **Plan** (smart, 1/defect): candidate + corroboration + enclosing func text → `{target,intent≤300,test_name,test_sketch≤600,blast_radius}`. No writes. → Drain.
- **Drain** (Go+craftsman, AS BUILT): snapshot→craftsman writes→empty-diff=FAIL→`go build ./...`→touched-pkg -race→fail⇒revert+1 retry⇒2nd fail⇒mark FAILED, keep revert, advance. → Audit.
- **Audit** (Go): re-run each fixed defect's lens over CHANGED FILES ONLY + exported-surface diff. New candidate in changed files → back to Judge for THOSE ONLY, auditRound++.
- **Close**: unconditionally reached — the return path, also via `defer` on error/panic/cancel.

**Deterministic termination (not a turn cap):** monotone shrinking set — auditRound≤2 AND re-judge only candidates in Drain-touched files AND any Finding.Key already ruled is auto-skipped (key set only grows). Terminates in ≤2 extra rounds regardless of model behavior; round-3 findings → UNRESOLVED in report. Termination is a property of the data structure.

## Q2. MODEL CONTRACT
- Transport: one request = one JSON answer. GBNF grammar-constrained (llama.cpp supports it — strongest local "forbid anything else"). NO tools in any judgment request. MaxTokens per schema (verdict 400, plan 1500, report 3000). Strict decode (DisallowUnknownFields), 2 retries w/ parse error appended, then schema-default fallback (CANT_TELL / all-lenses / templated report). Model physically cannot act — no tools; all side effects are Go's.
    type Caller interface { Ask(ctx, tier Tier, prompt string, grammar GBNF, out any, opts ...AskOption) error }
    // Amendment (2026-07-23, step-3 build): the sketch's 5-arg Ask could not honor
    // "MaxTokens per schema" (this section's own clause) — the caller can't see the
    // schema. A variadic ...AskOption (caller.WithMaxTokens) carries it without widening
    // the core contract; every documented 5-arg call still compiles verbatim. The
    // schema-typed FALLBACK (CANT_TELL / all-lenses / templated report) is likewise
    // schema-specific, so it lives with the CONSUMER: Ask does GBNF + strict-decode + 2
    // parse-retries (bad output + error appended) and returns a wrapped ErrExhausted; the
    // state machine maps that to the right default. Transport (429/5xx) + ctx errors
    // return directly (re-asking can't fix them).
- **Judge** (fast): input = 2-sentence lens desc + numbered-line snippet + MechanicalFacts ("arg 2 line 41 is literal nil; binds to param scope *Session per go/types") + 2-4 fixed contrastive exemplars (generic defect/correct shapes). Asymmetric prior: "enumerator over-generates; most are CORRECT; DEFECT requires naming the exact failing line."
- **PairDiscriminate** (fast): both snippets A/B + scope question → `{a:SAFE|UNSAFE, b:..., a_scope_source_line, b_scope_source_line}`.
- **Refute** (fast): `{stands, refuting_line}`.
- **Orient/Plan/Report** (smart): typed as above; Report is DECORATIVE — Go emits templated report from ledger on any failure. Never load-bearing.
- **Craftsman**: stays a subprocess (edit-compile-fix is genuinely iterative), one-step mandate (step + enclosing file), RESULT: DONE|FAILED, verified by build regardless (never trusted).

## Q3. MACHINE-FINDS / MODEL-JUDGES (~90% of hunt is AST-enumerable)
Fully AST-enumerable (model only classifies):
- **Tautology (D1):** constant-fold boolean exprs under go/types — `len(x)>=0`, `x==x`, `uint<0`, `||true-arm`. D1 caught purely mechanically; verdict pre-seeded DEFECT, model asked only to confirm decisive line, disagreement escalates (never dismisses).
- **Nil-scope-arg (D2/FP):** enumerate calls passing nil/zero to a pointer param where MOST sites pass a derived value (NO name-matching needed). Both D2 AND FP enumerate — correct: enumeration is RECALL, judgment is PRECISION.
- Unlocked-shared-state (callgraph: pkg var/field written from goroutine/handler w/o dominating Lock), cleared-before-flush (statement-order), ignored-error, shadowed-return, defer-in-loop — all mechanical, over-approximate is fine.

Needs semantic judgment: is a nil arg intentional (broadcast-all) vs leak — exactly D2 vs FP. go/types supplies the discriminating facts (statesess nil'd param = `scope *Session` per-session broadcast; stateapp nil'd = `app *App` app-wide) + callee doc + sibling call sites. Model answers a forced contrastive question, not raw shape-match.

Designs out all three at once: over-flag (model can't grow candidate set; DEFECT survives 3 independent filters); fabrication (Go supplies quote — nothing to fabricate); D2-vs-FP (recall mechanical, precision = easiest forced choice).

## Q4. TERMINATION & CLOSE
Close = the run's exit, via defer off `context.Background()` (runs even on cancel/panic):
    defer func(){ if r:=recover();r!=nil{err=...}; rep=o.mechanicalClose(context.Background(), err) }()
mechanicalClose pure Go: gofmt -l, go vet ./..., go build ./..., go test ./... -count=1 (touched -race), git diff --stat, git status --porcelain, exported-surface diff vs Index snapshot, ledger dump w/ dispositions, verbatim exit codes. Then optional smart prose call; templated fallback. Zero turns from any budget, own 10-min wall clock → cannot be starved. Mode 4 impossible: even a mid-Drain crash produces a close record; Drain never leaves a broken build resident.
Budgets PER-PHASE not global: Budget{ModelCalls, WallClock}; exhaustion → record PHASE_TRUNCATED and ADVANCE, never abort.

## Q5. WEAK-MODEL ROBUSTNESS (transport/orchestrator, all Go)
1. Output ceiling: switch to STREAMING; hard-kill past MaxTokens (belt) + per-schema byte ceiling (suspenders).
2. Repetition kill: rolling 256-byte window, best-repeated-substring ratio >0.9 for 3 windows → cancel, retry once at temp+0.3, then fallback. INPUT guard: prompt assembly returns (prompt,tokens,error), errors at the wall (the 2.86M blowup was unbounded prompt assembly).
3. Context accountant: fit(parts,budget) per-call token count; budget per class (judge 6k, plan 16k, report 24k); structured truncation (drop exemplars→context lines→NEVER candidate snippet). Stateless calls → no growing conversation.
4. Schema enforcement: GBNF + strict-decode + 2 retries + typed fallback (degrade toward doing LESS, safely).
5. Slot hygiene: sem(4); per-request wall clock (fast 120s, smart 300s); craftsman keeps idle watchdog + 32KB cap + 12-turn cap w/ revert-on-cap.
6. No model-reachable side effects: judge/refute/plan/report carry no tools; craftsman only touches disk, in a snapshot, verified by build. Modes 1&3 unrepresentable (no leader loop, no optional lens, no tool to choose).

## Q6. KEEP vs SCRAP — replace, don't wrap, keep the seams
Hybrid worth having: "deterministic orchestrator + ONE agentic role (craftsman) as bounded subprocess + build flag swapping the whole orchestrator for old Loop on frontier."
- **Scrap** (local path): leader agent loop, all 3 deny-and-steer gates (no target once model has no tools), Unfinished() nudge, Editor sweep (no resident leader ctx), MaxTurns global cap, HuntTool as model-driven activity.
- **Keep verbatim**: quote-gate (→decisive-line validator), Drain (the template for everything), Finding/Ruling/Step ledger + Finding.Key (the termination proof's data structure), scout/craftsman subprocess runner + sem(4) + watchdog, CloseTool checklist (→mechanicalClose function), V| polarity (→CORRECT/DEFECT enum).
- **Keep behind flag**: `--driver=agentic|deterministic`. Frontier=Loop.Run, local=state machine. Shared: transport, ledger, drain, close. Also the cleanest methodology A/B.
Why NOT wrap agentic inner steps for judge/orient: any inner agentic step re-imports the gate problem in miniature. Agency only pays for WRITING code (craftsman); everywhere else it was an expensive way to phrase a question Go can phrase exactly.

## BUILD ORDER (fastest path to D1+D2 caught, FP skipped, always closes)
1. **defer mechanicalClose** around whatever driver runs (½ day; kills mode 4 NOW, even before new driver — promote CloseTool checklist to a plain func).
2. **Transport hardening** (streaming + output ceiling + repetition kill + prompt-size wall) — small, protects everything, kills mode 6.
3. **Caller.Ask + strict-decode + GBNF** for Verdict/Pair/Refute — typed-call substrate.
4. **Two lenses only**: tautology (D1) + nil-scope-arg (D2/FP) as tree-sitter `.scm` queries over gotreesitter (Amendment 1; NOT go/ast) → Candidates w/ MechanicalFacts. Syntactic facts come from the query captures; semantic facts (e.g. the nil'd param's declared type — D2 `*Session` vs FP `*App`) come from a fast-agent summary of the extracted CST slice (Amendment 2), not go/types. Skip index/callgraph generality; per-file parse of focus pkgs suffices.
5. **State-machine skeleton**: Index-lite→Enumerate(2)→Judge→Corroborate(line-check+pair+refute)→reuse Plan/Drain→Audit(bounded)→Close. Hardcode lens selection (skip Orient) for first run.
6. **Acceptance run**: success = ledger shows D1 DEFECT-fixed, D2 DEFECT-fixed, FP CORRECT-ruled, build green in close record. Achievable steps 1-5, no smart calls except Plan.
7. Generalize: Orient, remaining lenses, smart escalation, audit re-entry, --driver flag + A/B.

Steps 1-2 land even if the rest is debated — they fix verified failure modes in the CURRENT architecture.

---

## AMENDMENTS (user direction, 2026-07-23)

**MULTI-LANGUAGE, not Go-specific.** The harness must be language/format-agnostic from the start; `go/ast`+`go/types` would make it Go-only. Decisions:

1. **Enumeration substrate = tree-sitter, non-CGO — SETTLED (2026-07-23).** A lens = a named tree-sitter query (`.scm`), one query-set per language; uniform CST API across languages. Syntactic lenses (tautology, nil-arg patterns) are plain query matches. **Route: `github.com/odvcencio/gotreesitter`** — a pure-Go tree-sitter (parser + full `.scm` query engine; per-language `XxxLanguage()` grammar accessors, Go/Python/JS/Rust incl.). Self-verified: 0 C files, 0 `import "C"`, builds to a statically-linked binary under `CGO_ENABLED=0`, and a smoke test parsed Go + ran `.scm` queries catching the D1 tautology (`len(x) >= 0`) and D2 nil-arg (`f(ctx, nil, key)`) shapes. The cgo bindings (smacker, tree-sitter/go-tree-sitter) and the WASM-under-`wazero` fallback are NOT needed. API shape: `grammars.GoLanguage()` → `gts.NewParser(lang).Parse(src)` **returns `(tree, err)`** (two values — corrected 2026-07-23, fable-verified against v0.47.0; the earlier single-value sketch would not compile) → `gts.NewQuery(scm, lang).Execute(tree)` returning `[]QueryMatch{Captures:[]QueryCapture{Name string; Node *gts.Node}}`; node text = `src[n.StartByte():n.EndByte()]`; node kind = `n.Type(lang)`. Predicate set confirmed supported: `#eq? #match? #any-of? #has-ancestor? #count?`. Gate behavior confirmed: unknown node type / unknown field name / unbalanced parens all error at `NewQuery` (compile-time). **NOT yet pinned in go.mod** (self-verified only in the module cache, v0.47.0) — the earlier "pinned; go.mod is the source of truth" claim was FALSE; pin it in the first `internal/enumerate` commit that imports it.
2. **Semantic layer = fast-agent summarizer EVERYWHERE** (chosen). Tree-sitter is syntactic only; the type/binding facts go/types would give (e.g. D2's nil'd param is `scope *Session` vs FP's `app *App`) come uniformly from a fast agent reading the specific CST slice (callee sig + enclosing fn, extracted by node byte-ranges) and summarizing into a typed schema. Low-fabrication (summarizing REAL extracted text), bounded (one slice). One uniform code path, no per-language semantic providers. `SemanticProvider` iface with a go/types opt-in was REJECTED in favor of uniformity.
3. **Build scope = SAFE WINS FIRST** (chosen). Landed this session (both language-agnostic, both fix verified failure modes in the CURRENT agentic arch):
   - **defer MechanicalClose** (leader/close.go + cmd/main.go): pure-Go close-out record (detected suite + git diff/status + 0-byte find, verbatim exit codes) runs in a `defer` off context.Background() whenever no clean close-out ran → close can never be starved by the turn cap. Kills failure mode 4.
   - **Transport prompt-size wall + output default cap** (api/openai.go): refuse to SEND a request whose estimated prompt exceeds maxPromptTokens=200k (the 2.86M-token blowup was unbounded INPUT assembly); default MaxTokens=8192 when absent. Kills failure mode 6's input side. DEFERRED (needs switch off the non-streaming client): streaming + mid-generation output ceiling + repetition kill-switch.

**Progress (2026-07-23):** safe wins landed and the working tree was cleaned — the abandoned agentic prompt-gate experiment (read-gate/hunt-gate/scout-unification) was discarded back to HEAD, junk binary removed + gitignored. Tree green (gofmt/vet/build/test all pass). Substrate settled (Amendment 1). **`internal/enumerate` package LANDED (build-order step 4):** two lens backends (`QueryLens` .scm shape + `WalkLens` Go-CST order — `StmtAfterReturn` proves the order backend), injection-safe `Template` parameter-fill (tier 1) + free-form `.scm` (tier 2), the witness `Gate` (positive-must-match / negative-must-not, dropping vacuous+over-broad), the `Registry` coverage floor (`BuildBaseline` untouched by derived outcome; `AcceptDerived` records every `DERIVE_FAILED` and never aborts), and `Run` (baseline always applies; derived faces the selectivity ceiling, all truncation/drops recorded in `Notes`). gotreesitter v0.47.0 now pinned in go.mod. Close scout: 13/13 packages pass, enumerate race-clean, vet+gofmt clean, no 0-byte files. The build-fix loop caught two real grammar-vocabulary errors (`nil` is its own node kind not `identifier`; statements live under `statement_list` not `block`) — exactly the failure class the witness/compile gates exist to catch.

4. **DYNAMIC LENSES — the model DERIVES lenses from intent+terrain; static seeded `.scm` lenses are REJECTED as lens-circular (user direction + fable counsel, 2026-07-23).** Hand-tuning a query to a known defect (`len(x)>=0`, `f(ctx,nil,key)`) proves nothing about recall on unseen code — this project already measured a rev1-tailored lens set score 6/6 then collapse to 3.7/6 when generalized. So Enumerate's lens set is a FUNCTION of (intent, terrain), authored at recon, not a fixed library. This is nullius's own recon-names-terrain → one-lens-per-hunter shape, with the recall layer made deterministic.

   **Revised early flow (supersedes the "Index → Orient → Enumerate" head of Q1 for the local path):**

       Orient (smart×1: analyze intent + mechanical terrain digest)
        → GATE (three-way classification, FAIL-CLOSED)
        → Recon (smart×1: DERIVE lenses → emit queries/params, FROZEN after this phase)
        → [Enumerate → Judge → Corroborate → Plan → Drain → Audit → Close as before]

   - **Three-way gate — ANSWER / BUILD / FIX** (generalizes the FULL/BUILD gate; adds a read-only exit). Classification is fail-closed exactly as nullius's "doubt → FULL": **any in-scope pre-existing code ⇒ default FIX**; ANSWER/BUILD require a quoted mechanical absence of in-scope code. Misclassifying FIX→ANSWER skips the whole hunt with no backstop, so the bias is mandatory, not cosmetic.
     - **ANSWER** — pure question, no mutation. Terrain read → report. No Enumerate.
     - **BUILD** — greenfield, no inherited terrain to bite. NOT a new path: the machine entered at Plan with an empty candidate set; Audit's existing post-build re-hunt over changed files covers the new code (this IS nullius BUILD mode's "hunt your own new code before close").
     - **FIX** — brownfield, terrain has targets → Recon derives lenses, then the normal machine.

   - **THE COVERAGE-FLOOR RULE (load-bearing — this is what keeps the thesis intact).** Tree-sitter makes the *evidence* mechanical (no fabricated hits), NOT *recall* — recall is *which queries exist*, which the model now authors. Defensible ONLY under: **model-derived lenses may only ADD to a baseline coverage floor they can never subtract from; a failed derivation degrades to baseline-only, recorded `DERIVE_FAILED`, never silently.** Worst case = not having the feature. Without this we rebuild the softened hunt nullius measured at 5/6.

   - **Two authoring tiers, both behind a WITNESS GATE (user chose: ship both now).**
     - **Tier 1 — parameter-fill over pre-verified templates (preferred).** The mandate-specific part of a lens is almost never a novel tree *shape* — it's *which names / which arg-index / which literal*. Ship hand-verified `.scm` templates with holes filled via `#match?`/`#any-of?`; the smart model fills PARAMETERS (`{template:"literal-arg-to-call", fn_regex:..., arg_index:2, literal:"nil"}`), not syntax. This makes derivation a forced choice over concrete alternatives — the machine's native move — and eliminates the syntactic-authoring risk class (the substituted query is still `NewQuery`-backstopped).
     - **Tier 2 — free-form `.scm` escalation, flagged, when no template fits.** Weak-model risk is real; the gates below make it survivable.
     - **WITNESS GATE (every derived query, both tiers).** The model must emit, per query: (a) a minimal POSITIVE snippet it MUST match, (b) a minimal NEGATIVE it must NOT, (c) a mechanism token for Corroborate filter-1. Go parses both snippets and runs the query: positive-miss or negative-hit ⇒ lens REJECTED, mechanically. This is rule-5 ("name the change that flips it red; none → vacuous, cut it") applied to lenses. Plus a **selectivity ceiling**: raw hits > N or > X% of candidate nodes ⇒ reject as non-discriminating (guards the over-broad flood, which otherwise dilutes the very attention the design protects). Syntax / wrong-node-name errors are already gated by `NewQuery` → retry with the verbatim error appended (2×) → drop. Un-gated without witnesses: vacuous-valid (empty query compiles, returns 0), over-broad flood, plausible-but-wrong mechanism — the witness gate converts all three from silent to caught.
     - Also feed the derivation prompt a **mechanically-extracted node-kind inventory of the actual terrain files** (Go dumps the kinds present in the parsed trees) — grounds the weak model in the real grammar vocabulary instead of its priors.

   - **THE SYNTACTIC CEILING (do not paper over).** Tree-sitter has no dataflow / dominance / statement-order. This project's hardest *measured* misses — sse-premature-clear (missed 4/4 by open sweeps) and the fault-survival lens ("cleared/overwritten before its write, send, or flush") — are lifecycle/order properties `.scm` CANNOT express. Those need small pure-Go CST algorithms (a sibling-order walk over statements is trivial), parameterized by the model (which buffer, which flush call), NOT queries. Without this, recon looks strongest where defects are easiest and blind where benchmarks say the bodies are buried. So the lens engine has TWO backends from the start: `.scm` queries (shape) + CST walks (order/lifecycle), both parameterizable, both witness-gated.
     - **The walk backend is LANGUAGE-AGNOSTIC via `LangProfile` (2026-07-23), not Go-hardcoded.** A walk body reasons over abstract `Role`s (Function, Closure, Body, StmtList, Return, Loop, Call, Defer) resolved per-language by a small declarative table; the SAME walk runs on any language by swapping the profile — the walk analogue of what the query engine does for `.scm`. Adding a language = one hand-written table, not new walk code. **Closures are first-class**: a func literal is a Function BOUNDARY (so a return/defer inside it is never attributed to the enclosing scope) but anonymous (skipped for naming). Bootstrap-from-tags.scm was REJECTED by fact: gotreesitter v0.47.0 ships EMPTY `TagsQuery` for all 8 languages checked (highlight queries are populated but too coarse to separate control-flow roles). When languages are added, revisit vendoring upstream tags.scm vs deriving from highlights; profiles are hand-written for now (Go only).

   - **Composition & staleness.** (1) Baseline ALWAYS runs; Recon can only add. (2) A derived lens whose hits produce a confirmed FIXED is PROMOTED into the versioned baseline with its witness snippets as regression tests — the "rev3 always-on lens library" compounding instead of rotting. (3) In benchmark arms, firewall any defect-descriptive seed text out of the recon prompt (intent+terrain is aim; a seed description is bench-9 circularity). (4) Corroborate filter-1 (decisive-line mechanism token) is vacuous for derived lenses unless the witness token (c) is carried — so it is required in the schema. (5) The cap-30-by-severity rule is undefined for derived lenses (no severity model) → give them an explicit ordering (witness-similarity or file-locality), else the cap is silent recall loss.

   - **Termination unchanged IF queries FREEZE at Recon.** The ≤2-round proof is monotone-shrinking over a fixed candidate universe; if Audit could re-derive queries the universe grows and the proof dies. Derived queries freeze after Recon; Audit re-runs the SAME frozen set over changed files only.

   Semantic facts (the nil'd param's declared type — D2 `*Session` vs FP `*App`) still come from the fast-agent CST-slice summary (Amendment 2), not go/types. Skip index/callgraph generality; per-file parse of focus pkgs suffices.

5. **State-machine skeleton**: Orient→Gate→Recon→Enumerate→Judge→Corroborate(line-check+pair+refute)→reuse Plan/Drain→Audit(bounded, frozen lenses)→Close.
6. **Acceptance run** — must AVOID lens-circularity: success is NOT "did the seeded D1/D2 get caught by a query written for them". Success = over a fixture the recon prompt has NOT seen the seed descriptions of, the ledger shows defects enumerated → judged → confirmed/refuted with the coverage floor intact and build green in the close record; a `DERIVE_FAILED` still closes cleanly on baseline-only. Measure recall honestly (seeded defects vs caught) AND the derived-lens witness-gate reject rate on the weak local model — the latter is the real unknown.
7. Generalize: remaining baseline lenses, smart escalation, promotion pipeline, --driver flag + A/B.

**`internal/caller` package LANDED (build-order step 3, 2026-07-23):** the typed model-call substrate — `HTTPCaller.Ask(ctx, tier, prompt, grammar, out, ...opts)` over per-tier llama.cpp endpoints (Fast/Smart). One request → one grammar-constrained JSON answer: GBNF forwarded verbatim, `temperature 0`, `stream false`, and NO `tools` field on the wire (the model has no side-effect channel by construction — verified by a test asserting the absence). Strict decode (`DisallowUnknownFields` + trailing-content check) into the caller's struct; a schema-invalid reply is re-asked up to 2× with its own bad output + the parse error appended, then `ErrExhausted` is wrapped for the consumer's typed fallback. Transport 429/5xx get bounded backoff retries; ctx/ prompt-size wall abort before/without re-asking. 9/9 package tests pass, race-clean.

**`internal/machine` LANDED — step-5 SKELETON, front half live-verified (2026-07-23).** The dynamic-lens front half (Orient→Gate→Recon→Enumerate) is wired over the caller and run end-to-end against the live local endpoints (`cmd/nullius-det`, an observable per-phase-streaming CLI). The back half (Judge→Corroborate→Plan→Drain→Audit→Close) is walked as marked stubs. Typed structs + prompts for each smart phase; a shared well-formed-JSON GBNF (per-phase grammar tightening deferred to step 7); every smart phase fails CLOSED (Orient→empty, Gate→FIX, Recon→baseline-only). `normalizeMode` enforces the fail-closed gate with a MECHANICAL backstop: any declared function/method in the terrain ⇒ FIX regardless of the model's claim. Terrain digest = node-kind histogram + fn names of the actual parsed trees (grounds the weak model in real grammar vocabulary).

**Live findings (three runs vs internal/ledger/ledger.go, a real mutex-guarded file):**
- The caller's `reasoning_content` fallback is LOAD-BEARING: these local models (minimax-m2.7, qwen3.6) are reasoning models that route grammar-constrained JSON into `reasoning_content`, leaving `content` empty. Without the fallback every phase ErrExhausts.
- The WITNESS GATE is empirically validated as the recall-honesty mechanism: run 1 rejected 5/5 malformed lenses; run 3 accepted 7/8 and rejected the one bad one (`fn_regex="^go$"` — `go` statements are not call_expressions). Coverage floor held on every run — a failed derivation degraded to baseline-only, never a false pass.
- Two prompt/tuning gaps found and fixed live: (a) `templateDoc` now documents that `@fn` captures the WHOLE called expression (`recv.Method`, not `Method`), because the model kept anchoring `^Method$` regexes that can't match a selector; (b) reasoning models spend the output budget on `reasoning_content` where the JSON also lives, so the per-phase caps needed real headroom (Orient 2500 / Gate 1500 / Recon 8000) — an under-budgeted reply truncates to `unexpected EOF` and silently costs coverage. After both fixes: Recon derived 8 concurrency lenses, Enumerate returned 20 real candidates (every Lock/Unlock site, fn-attributed).

**Judge + Corroborate LANDED — step-6 discrimination half, live-verified (2026-07-23).** `internal/machine/judge.go`: per-candidate `Judge` (fast, forced choice DEFECT|CORRECT|CANT_TELL + decisive_line + because) → `Corroborate` (filter 1: decisive line must be inside the offered evidence window and non-blank, else CANT_TELL; filter 3: refuter). Candidate SITES become confirmed RULINGS. CLI (`cmd/nullius-det`) prints per-candidate rulings + a CONFIRMED count. Pair-discrimination (filter 2) + smart escalation deferred to the fix-pipeline increment.

**Two live findings (fixture: unreachable-code function + clean twins), both drove fixes/confirmations:**
- **Fail-closed survived a whole-tier outage.** The smart endpoint (:8080) went down mid-run; Orient/Gate/Recon all fell back (empty/FIX/baseline-only) and the pipeline kept going on the fast tier — Enumerate found the unreachable code, Judge+Corroborate ran. Graceful degradation under real infra loss, no crash, no false pass.
- **The refuter needed an evidence gate (doctrine fix).** The weak fast refuter overturned genuine unreachable code with `refuting_line=null` or `=the flagged line itself` — a bare "stands=false" clearing a true defect. Applied nullius's "a REFUTED ruling is never a bare word": a refutation now overturns a judge-affirmed DEFECT only if it cites a VALID, DISTINCT safety line; otherwise the defect STANDS. After the fix the self-line refutation was rejected → the defect CONFIRMED. (A second candidate hit a transient server-side GBNF crash — llama.cpp 500 `"Unexpected empty grammar stack"` — which the caller handled fail-closed → UNVERIFIED, never a false confirm. RISK: the local grammar engine is not 100% reliable; a step-7 mitigation is to retry a grammar-500 unconstrained + strict-decode.)

**Plan + report-Close LANDED — live-verified (2026-07-23).** `internal/machine/plan.go`: `Plan` (smart, one `PlanOut{target,intent,test_name,test_sketch,blast_radius}` per confirmed defect, NO writes); a smart failure degrades to a mechanical templated plan flagged `Fallback` (a defect never silently loses its plan). `Result.Plans`; the machine now runs the full read-only pipeline find→discriminate→confirm→plan→report and the CLI prints plans. Live run (fixture): both unreachable statements CONFIRMED (refuter-evidence gate rejected a self-line AND a null-line false refutation), 2 plans produced. The smart endpoint dropped mid-run AGAIN (it is flaky) → Gate/Recon/Plan all hit fallbacks and the pipeline STILL confirmed both real defects and emitted honest FALLBACK plans — fail-closed proven under repeated real outage.

**Drain + Audit LANDED — the full pipeline PASSED a live acceptance run (2026-07-23).** Fork resolved by the user: **option A** — reuse the proven `--craftsman` subprocess. `internal/machine/drain.go`: a `Writer` interface (prod `SubprocessCraftsman` shells out to `go-nullius -p <objective> --model fast --craftsman --dir <root>`; fakes for tests), its own git snapshot/revert (mirrors `internal/leader/gitstep`, unexported there), and the safety loop — snapshot → craftsman writes → **empty diff = FAIL** → `go build ./...` → touched-package `-race` → any failure REVERTS and retries once with the failure fed back → second failure marks FAILED and leaves the tree reverted. `Audit` re-runs the FROZEN lens set over the (now-modified) files and reports residuals. Drain runs only with a `Craftsman` AND `Mandate.Dir` set; otherwise plans are reported, not written. CLI: `-dir` + `-craftsman-bin` enable it.

**Live acceptance (fixture: a `Sum` fn with two unreachable statements after `return`, git-tracked):** the deterministic pipeline ran END-TO-END on the local models and FIXED the code — Orient→Gate(FIX)→Recon(derived lens witness-failed, baseline covered it)→Enumerate(2)→Judge(both DEFECT, accurate)→Corroborate(refuter-evidence gate rejected both false refutations → 2/2 CONFIRMED)→Plan(2 detailed plans)→Drain(craftsman removed the dead lines + added a test, mechanically verified build + `./... -race`, 2/2 DONE)→Audit(0 residual)→Close. `git diff` confirmed the real edit. The full thesis — Go finds, model discriminates, craftsman writes under a mechanical net — holds on weak local models. Rough edge (known): the smart Plan over-decomposed one fix into two overlapping same-file plans; both drained DONE but the second was redundant (plan-overlap warning is a step-7 add).

**D2-vs-FP DISCRIMINATION PROVEN live (2026-07-23).** Fixture: a `Store` with `Get` (Lock + `defer Unlock` — correct) and `Set` (Lock, no Unlock — a real deadlock defect), same lock shape. The pipeline derived a `method-call ^Lock$` lens, flagged both Lock sites, and JUDGE discriminated correctly — `Get`'s lock CORRECT ("defer Unlock ensures release"), `Set`'s lock DEFECT — then Corroborate CONFIRMED only `Set` (1/3), with a correct fix plan. The same-shape safe twin was cleared and the real leak confirmed, on weak local models, in 7.7s. Three findings got it there:
- **Method-name template** (`method-call`: `field: (field_identifier) @method`, regex `^Lock$`) — the robust fix for the recurring `@fn`-capture confusion (the model kept mangling `call-to`'s receiver-qualified regex).
- **Strict per-phase GBNF BACKFIRES on reasoning models** — a rigid Recon grammar gags the `reasoning_content` channel (where the constrained JSON lands), so the model can't deliberate and truncates (270s → unexpected EOF). Reverted. The working lever is a PERMISSIVE grammar (thinking room) + a SIMPLE requested schema (5 keys per lens, `params` explicitly an object) — after which Recon derived valid lenses reliably.
- **Evidence window must be the ENCLOSING FUNCTION, not ±N lines** — a fixed radius spanned into the sibling function and let the refuter clear `Set`'s missing-Unlock by citing `Get`'s `defer Unlock`. Scoping Judge/refuter/Plan to the enclosing function (via `RoleFunction` node spans) fixed it. Also fixed a real bug: partial-decoded garbage was fed to `AcceptDerived` on a Recon parse failure.

**Landed alongside D2-vs-FP (2026-07-23):** an env-gated live acceptance test `TestLiveAcceptance_D2vsFP` (NULLIUS_LIVE_ACCEPT=1) pins the discrimination against the real models — PASSED live (11.7s: Set confirmed, Get cleared); the hermetic fake-caller tests still pin the logic offline. Plus `deferred-method-call` template (the model had reached for a nonexistent `defer_statement`), and **plan-overlap dedup** (`dedupeConfirmed`: same file+fn+lens defects collapse to one plan target — fixes the measured over-decomposition).

**Confirmed-derived→library promotion LANDED (2026-07-23).** A model-derived lens that produces a CONFIRMED defect (survives Judge + Corroborate) is persisted to an on-disk lens library (`machine.LensStore`, `<dir>/.nullius/lenses.json`, keyed by language, already gitignored). Future runs SEED those specs as always-on derived lenses — the model no longer has to re-derive a lens that has already proven itself, directly de-risking the Recon flakiness. Design choices: (a) seeds are re-witness-gated AND kept under the selectivity ceiling every run because they are model-authored — a spec that goes stale (template removed, grammar drift) drops harmlessly, never trusted blindly (nullius in verba applied to the pipeline's own memory); they are NOT promoted to true baseline (which is pre-verified and exempt from both nets). (b) Merge is seed-first, deduped by ID, so a confirmed candidate's lens ID resolves to exactly one spec. (c) Baseline lenses are never promoted (they're already the floor); already-seeded lenses are never re-promoted. (d) Best-effort persistence: a save failure is logged, never fatal — the run's rulings stand regardless. Pinned by 7 tests: store roundtrip/dedup/lang-scope, seed-merge ID tracking, end-to-end promotion of a confirmed derived lens (baseline excluded), and a second run enumerating purely from a seed with the model deriving nothing.

**Grammar-500 unconstrained retry LANDED (2026-07-23).** llama.cpp intermittently 5xx's on a valid GBNF (observed: "Unexpected empty grammar stack"). This is DETERMINISTIC — the same grammar re-crashes — so backoff is wasted. `caller.post` now fast-fails such a 5xx (a 5xx whose body contains "grammar", when a grammar was set) with the sentinel `ErrGrammarCrash`; `Ask` catches it and retries ONCE unconstrained (grammar dropped for the rest of that Ask), with strict-decode + parseRetries still enforcing the schema. A generic 5xx (no "grammar" in the body) is unaffected — it keeps the normal backoff path with the grammar intact. 3 tests: recover-on-crash (verifies the retry body is unconstrained), persistent-crash tries the constrained grammar exactly once (no backoff loop), generic-500 never drops the grammar. This removes the one UNVERIFIED failure mode seen in the D2-vs-FP run.

**Pair-discrimination (Corroborate filter 2) + bounded smart escalation LANDED (2026-07-24).** Per-candidate Judge is blind to context, so two structurally-identical sites (D2 vs FP) both come back CANT_TELL. `pairDiscriminate` groups the still-unresolved CANT_TELL candidates by lens and asks ONE contrastive fast call per same-lens pair showing BOTH enclosing windows: which is SAFE, which UNSAFE, and cite where each site's decisive value originates (`PairOut{a,b,a_scope_source_line,b_scope_source_line}`). A differentiated answer (one SAFE, one UNSAFE) whose UNSAFE side cites a VALID origin line CONFIRMS the UNSAFE site and clears the other; an undifferentiated answer (equal verdicts, or an unsupported origin) escalates the pair to the smart tier, budget ≤3/run (the flaky/expensive tier is rationed); a still-undifferentiated smart answer leaves both unconfirmed (fail-closed → RISK). Runs before Confirmed() so a pair-confirmed defect flows into promotion + Plan like any other. Lens keys sorted for deterministic budget consumption. 4 tests: confirm-unsafe/clear-safe, unsupported-origin confirms nothing, undifferentiated escalates to smart exactly once, escalation budget caps smart calls at 3.

**Bounded audit→judge re-entry LANDED (2026-07-24).** `auditReentry` replaces the log-only `audit`: after Drain it re-hunts the frozen lens set and, for any FRESH target (a file+fn+lens not already ruled this run — one a fix introduced or exposed), re-judges it and drains newly-confirmed defects, looping up to `maxAuditRounds=2`. Termination is guaranteed by a monotonically-growing seen-set keyed by `targetKey` (file+fn+lens, stable across fix-induced line shifts) under a hard round cap — each target processed at most once. A residual at an already-ruled target (fix didn't remove the shape) is surfaced as a "fix incomplete, RISK", never re-drained (drain already tried+reverted). 2 tests: a fix that introduces a fresh defect in a new function is caught and drained in the re-entry round; a persistent residual at a ruled target is surfaced as a RISK without oscillating.

**`--driver=agentic|deterministic` DESCOPED (2026-07-24, user decision "only deterministic").** The deterministic state machine IS the driver; there is no agentic-vs-deterministic flag or methodology A/B. The v0 agentic path (`cmd/go-nullius`, frontier Loop.Run) is left in place but vestigial — not wired to a driver selector. Step 7 is complete.

**Coverage-floor build-out — increment 1 (2026-07-24, fable-advised, from the vialite bench).** The vialite bench (1/6 recall, see `benchmarks/2026-07-24-go-nullius-deterministic-vialite.md`) showed the specced coverage floor (DESIGN:57-59) was never built — only `stmt-after-return` shipped — and that off-lens judge rulings were poisoning the promotion library. Landed:
- **`bool-tautology` baseline lens** (`enumerate.BoolTautology`): constant-by-construction comparisons — `len(x)>=0`/`<0`, `0<=len(x)`/`0>`, and identical-operand comparisons (`x==x`, `i<i`). Task-agnostic class coverage (the line between legitimate baseline and lens-circular seeding: a lens encoding a defect *class* in language/stdlib vocabulary is baseline; one shaped to a known seeded site is circular). Catches always-true subscription predicates (the vialite over-wake miss). 3 tests.
- **Test-support file classification** (`machine.classifyFile`, in `BuildTerrain`): `*_test.go`→test; imports `"testing"`→testsupport (the strong signal — caught `backplanetest/conformance.go`, 2/3 of the bench FPs); terminal dir ∈{testdata,mocks,fakes,stubs}→testsupport. Excluded files leave `EnumFiles` but stay in `Files` (digest), recorded in `Notes` (never silent). Suffix/substring matching deliberately avoided (`latest/` trap). 2 tests.
- **Lens/judge coherence — Corroborate filter 1b RESTORED** (was specced at DESIGN:29, silently weakened to in-window+non-blank): every `Candidate` now carries an `Evidence []int` set of implicated lines (query lens = anchor+capture spans; walk lens = the flagged region), and a DEFECT's `decisive_line` must be `OnLens` (∈ Evidence). An in-window-but-off-lens ruling (the crypto.go FP: a json error ruled under a CAS lens) is downgraded to CANT_TELL — which also stops the promotion library being poisoned (it reinforces the lens of every confirmed candidate). `judgePrompt` constrained to the lens's concern; a serendipitous off-lens observation goes in `off_lens_note` as a LEAD (recorded, never confirmed/planned/promoted). 3 machine + 3 enumerate tests.

**Coverage-floor build-out — increment 2 (2026-07-24).** Three more GENERALIZED baseline lenses (deliberately NOT shaped to the vialite defect sites — that would be lens-circular cheating; each encodes a defect CLASS in language/stdlib vocabulary and runs unchanged on any codebase, accepting honest misses where a defect's shape doesn't match the class):
- **`lock-without-release`** (deadlock class): a `.Lock()`/`.RLock()` acquire in a function with no matching `.Unlock()`/`.RUnlock()` anywhere in that function. Keyed on the sync.(RW)Mutex API only. Over-approximates release presence across closures to keep FPs down. 3 tests.
- **`unguarded-field-write`** (missing-serialization class): a method that writes a receiver field of a struct declaring a `sync.(RW)Mutex` field, with no `Lock`/`RLock` in the method body. Guard signal = a field whose type text contains "Mutex". Over-approximate (constructors/init flagged, Judge clears). 3 tests.
- **`nil-literal-arg`** (scope-confinement / D2-vs-FP class): a literal `nil` argument where sibling calls of the SAME callee pass a non-nil value at that position — the contrast is the signal (a pure-optional nil, always nil, is skipped). BUILT + unit-tested but **HELD out of the always-on baseline**: measured on the bench (run3), it surfaces the right candidates but the solo fast judge confirmed the INVERSE — legitimate app-scoped `broadcastRender(nil,...)` calls ruled DEFECT while the real session-scoped leak was missed — because the D2 discrimination doesn't engage (pair-discrimination only fires on CANT_TELL ties; no callee-summary tells the judge which arg is a scope). A lens that confidently confirms FPs is worse than a miss (drain would "fix" correct code), so it stays unregistered until nil-arg verdicts are forced through pair-discrimination / fed the callee-summary. 2 tests.

**`clear-before-use` DEFERRED (not faked).** A syntactic walk cannot generalize it without either overfitting (keying on a specific clear helper) or flooding (`x = x[:0]` before reuse is the legitimate buffer-reuse idiom). It needs dataflow (a clear with no intervening reassign before a read), which the current CST-walk backend doesn't provide. Building a syntactic stand-in would be lens-circular or noise; left for a dataflow pass.

**REMAINING:** **sharded recon** (one recon call per fixed lens class → fixation structurally impossible), gated by fable behind new non-call templates (condition-shape, assignment-shape) + a richer terrain digest (imports + go/select/send/mutex-field counts + uncapped fn list). The CST-slice callee-summary feeding `judgePrompt` (cross-function D2 recall). And a dataflow backend for `clear-before-use`.
